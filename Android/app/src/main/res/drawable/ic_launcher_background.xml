<?xml version="1.0" encoding="utf-8"?>
<!-- Solid fill sampled from the app icon's own background green. Rarely visible on
     screen — the foreground bitmap (ic_launcher_foreground.png) is fully opaque and
     already carries its own gradient — this is just the adaptive-icon safety net. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#026A26"
        android:pathData="M0,0h108v108h-108z" />
</vector>

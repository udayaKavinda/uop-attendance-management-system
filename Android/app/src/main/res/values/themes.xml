<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme used before Compose takes over. Compose provides the real
         Material 3 theme (see ui/theme). We just need a NoActionBar shell with
         an edge-to-edge friendly, light background. -->
    <style name="Theme.Attendance" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/app_bg</item>
    </style>
</resources>
